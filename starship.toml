"$schema" = 'https://starship.rs/config-schema.json'

format = """
[](#5C1F55)\
$os\
$username\
[](bg:#8C3050 fg:#5C1F55)\
$directory\
[](fg:#8C3050 bg:#A85A35)\
$git_branch\
$git_status\
[](fg:#A85A35 bg:#3A6E8C)\
$python\
$c\
$cpp\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$rust\
$scala\
[](fg:#3A6E8C bg:#04595C)\
$docker_context\
[](fg:#04595C bg:#1A3A52)\
$time\
[](fg:#1A3A52)
$character
"""

# Disable the blank line at the start of the prompt
add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bg:#5C1F55 fg:#F0BBFF"
style_root = "bg:#5C1F55 fg:#F0BBFF"
format = '[$user ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bg:#5C1F55 fg:#F0BBFF"
disabled = false # Disabled by default

[os.symbols]
Macos = " "

[directory]
style = "bg:#8C3050 fg:#FFB3C8"
format = "[ $path ]($style)"
truncation_length = 3
truncation_symbol = ""

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[python]
symbol = ""
style = "bg:#3A6E8C fg:#C5E8FF"
format = "[ $symbol $virtualenv ]($style)"

[c]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[cpp]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bg:#04595C fg:#A8F0EE"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bg:#A85A35 fg:#FFD9B3"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bg:#A85A35 fg:#FFD9B3"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bg:#3A6E8C fg:#C5E8FF"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = false
time_format = "%R" # Hour:Minute Format
style = "bg:#1A3A52 fg:#B3D4F5"
format = '[ ⌛ $time ]($style)'

[character]
success_symbol = "[❯](bold white)"
error_symbol = "[✗](bold red)"
